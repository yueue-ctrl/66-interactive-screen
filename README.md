# square filter
An interactive experience built on a modular grid system for screen-based interaction. Users can dynamically adjust grid scale and graphic settings, import images or enable the camera, and generate real-time body contour visualizations.

Users can also upload images, which are automatically cropped to extract the portrait. The system then generates a character avatar with a dynamic background, which can be exported as a static badge.
